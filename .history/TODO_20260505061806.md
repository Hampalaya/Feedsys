# Feed System Data Visibility Fix - TODO Steps

## Plan Breakdown (Approved)
**Issue:** Data inserted in phpMyAdmin not showing in app → Backend not running / setup incomplete.

### Step 1: Verify DB Data [PENDING]
- In phpMyAdmin: Run `SELECT COUNT(*) FROM students;`
- Expected: >0 if data inserted manually.

### Step 2: Backend Setup & Start [PENDING]
```
cd backend
npm install
npm start
```
- Test: Visit http://localhost:3000/health → `{"status":"ok","message":"Database connected"}`

### Step 3: Frontend Start [PENDING]
```
# In project root
npm run dev
```
- Visit http://localhost:5173

### Step 4: Test App [PENDING]
- Login: Try username `admin` (password from users table).
- Check Students/Measurements pages.
- Browser: Clear cache, inspect Network tab for API calls.

### Step 5: Seed Data (if empty) [OPTIONAL]
```
mysql -u root feed_system < seed_data.sql
```
OR check `insert_seed.sh`.

---

**Progress:** Plan created. Follow steps in order. Reply when a step completes or issues arise.

