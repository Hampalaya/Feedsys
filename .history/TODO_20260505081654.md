# Fix Records Not Showing on localhost:5177 (CORS Issue)

**Status:** In Progress

## Steps:
- [✅] 1. Edit backend/server.js: Add 'http://localhost:5177' to CORS origins array
- [ ] 2. Restart backend server (`cd backend && npm run dev`)
- [ ] 3. Open http://localhost:5177 → Student Profiles page → Verify records load (check Network tab for /api/students 200 OK)
- [ ] 4. Mark steps complete and test other pages (Measurements, Attendance)

**Why:** Backend CORS allows 5173/5174 only. 5177 blocked → fetch fail → students=[] → empty UI.

**Backend/DB:** Confirmed working (/health OK, /api/students returns data).
